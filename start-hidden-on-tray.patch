diff --git a/build/main.js b/build/main.js
index a0df5b7..eeafbf6 100644
--- a/build/main.js
+++ b/build/main.js
@@ -3109,8 +3109,10 @@
                   finalMinSize[1]
                 );
             }
-            isDev() ? this.window.showInactive() : this.window.show(),
-              this.thumbar.init();
+            if (!process.argv.some(arg => arg === '--start-in-tray')) {
+                isDev() ? this.window.showInactive() : this.window.show(),
+                  this.thumbar.init();
+            }
           }),
           this.appService.enableDevMode();
       }
